let str = '';
str += '.flex-container{';
  str += 'display: flex;';
  str += 'flex-flow: row wrap;';
  str += 'justify-content: space-around';
str += '}';
str += '.navigation{';
  str += 'justify-content: space-around';
str += '}';

export default str;